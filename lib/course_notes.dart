import 'package:flutter/material.dart';
import 'package:app/home_screen.dart';
import 'home_screen.dart';
class course_notes extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("ok"),
      ),
    );
  }
}
